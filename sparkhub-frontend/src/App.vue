<script setup lang="ts">
// create-vue 默认导入了 RouterLink 和 RouterView，
// 如果您只用到了 RouterView，可以只保留它。
import { RouterView } from 'vue-router'
</script>

<template>
  <RouterView />
</template>

<style scoped>
/* 默认的样式 (例如 header, logo, nav 等) 都可以删掉了。
  您可以保留这个 <style> 块为空，或者彻底删除 <style> 标签。
*/
</style>

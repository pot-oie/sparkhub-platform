<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pot.sparkhub.mapper.FavoriteMapper">

    <select id="findFavoritesByUserId" resultType="com.pot.sparkhub.dto.ProjectSummaryDTO">
        SELECT
        <include refid="com.pot.sparkhub.mapper.ProjectMapper.ProjectSummaryColumns" />
        FROM
        user_favorite f
        INNER JOIN
        project p ON f.project_id = p.id
        LEFT JOIN
        user u ON p.creator_id = u.id
        LEFT JOIN
        category c ON p.category_id = c.id
        WHERE
        f.user_id = #{userId}
        ORDER BY
        f.create_time DESC
    </select>
</mapper>